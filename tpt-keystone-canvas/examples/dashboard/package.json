{
  "name": "tpt-canvas-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Demo dashboard mounting all six TPT Canvas components against a live tpt-keystone node (the crate's first-ever browser verification).",
  "scripts": {
    "build:wasm": "cargo build --target wasm32-unknown-unknown && wasm-bindgen target/wasm32-unknown-unknown/debug/tpt_canvas.wasm --out-dir pkg --target web",
    "mock": "node mock-server.mjs",
    "dev": "vite",
    "dev:mock": "vite --mode mock",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
