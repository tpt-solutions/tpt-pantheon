[package]
name = "ai-agent-demo"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
tpt-appfront-core = { path = "../../crates/tpt-appfront-core" }
tpt-appfront-ai-schema = { path = "../../crates/tpt-appfront-ai-schema" }
serde_json = "1"
