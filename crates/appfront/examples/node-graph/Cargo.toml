[package]
name = "node-graph"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
egui = "0.35"
eframe = { version = "0.35", default-features = false, features = [
    "default_fonts",
    "glow",
    "wayland",
    "x11",
] }
