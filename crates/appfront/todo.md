# TPT AppFront — Build Checklist

Architecture: one `UITree` + signal core, with DOM, Canvas, Webview (desktop shell), HTML(SSR), and AI-Schema as pluggable backend crates. See `spec.txt` for the original design doc.

## Phase 0 — Repo & Workspace Setup
- [x] `git init`
- [x] `.gitignore` (Rust/WASM/trunk artifacts)
- [x] README stub with quickstart pointer
- [x] Cargo workspace with empty crates: `tpt-appfront-core`, `tpt-appfront-dom`, `tpt-appfront-canvas`, `tpt-appfront-html`, `tpt-appfront-ai-schema`, `tpt-appfront-macros`, `tpt-appfront-cli`
- [x] GitHub Actions CI: `native` job builds+tests+clippies the workspace, `wasm` job builds+clippies `tpt-appfront-dom`/`tpt-appfront-canvas` on `wasm32-unknown-unknown`, `examples` job `trunk build`s everything under `examples/*/index.html` (`.github/workflows/ci.yml`)
- [x] CI dependency vulnerability scanning — `cargo audit` job (`taiki-e/install-action@cargo-audit`); added a `deny` job (`cargo-deny check` against a new root `deny.toml`: license allowlist, duplicate-version warn, denies unknown registries/git sources) for the license/duplicate-version checks that were the last gap here

## Phase 1 — Day 1: Reactive Signal System (`tpt-appfront-core`)
- [x] `Signal<T>` with `new/get/set`, `Rc<RefCell<T>>`-based, automatic dependency tracking
- [x] Unit tests: signal updates propagate only to dependents
- [x] `create_memo`/derived-signal caching + `batch()` + topological (rank-ordered) update scheduling — closes the diamond-dependency correctness gap (A→B, A→C, B→D, C→D updating A must run D exactly once); `set()` stays synchronous/opt-out-by-default, `batch()` is opt-in (`crates/tpt-appfront-core/src/signal.rs`)
- [x] `Signal::with(|v| ...)` / non-cloning borrow accessor — avoids the mandatory `T: Clone` cost on every `get()`, matters for large state (e.g. `DataGrid` row vectors); shares the same effect-tracking logic as `get()` via a private `track()` helper (`crates/tpt-appfront-core/src/signal.rs`)

## Phase 2 — Day 2: The UITree AST (`tpt-appfront-core`)
- [x] `UITree` enum: `Container`, `Text`, `Button`, `Input`, `List`, `Heading`, `DataGrid`, etc., each with `class`, `events`, `children`
- [x] `serde::Serialize`/`Deserialize` derive (generic over the app's `Msg` type)
- [x] Builder API matching spec's `UITree::container(|c| {...})` ergonomics
- [x] Virtual scrolling as a core `List`/`DataGrid` primitive (pulled forward from the Phase 5 optimizer-macro stretch goal) — `crates/tpt-appfront-core/src/virtual_scroll.rs`: `VirtualScroll` config (`item_height`/`viewport_height`/`scroll_offset`/`overscan`) + `visible_range(total_items) -> VisibleRange` computes the windowed index range and top/bottom pixel spacers for a fixed-height-item list; hung off `NodeMeta::virtual_scroll: Option<VirtualScroll>` / `NodeRef::virtual_scroll()`. `tpt-appfront-html`/`tpt-appfront-ai-schema` correctly ignore the field (render everything) since SSR/crawl/agent consumption needs the full list. Consumed by `tpt-appfront-dom` (see Phase 3); `tpt-appfront-canvas` doesn't consume it yet. *(uncommitted in working tree as of this writing)*

## Phase 3 — Day 3: DOM Backend (`tpt-appfront-dom`) — first web milestone
- [x] `web-sys`/`wasm-bindgen` renderer: `UITree` → real DOM nodes, minimal `create_element`/`set_text_content` calls
- [x] Event listener wiring (`onclick`) back to a `Msg` dispatch callback
- [x] Fine-grained updates: `reactive_text` ties a DOM text node directly to a `Signal<String>`, updates bypass the tree entirely
- [x] `examples/counter-dom` builds and serves via `trunk build` / `trunk serve` (verified: valid JS+WASM bundle, correct HTML shell)
- [x] `trunk`-based dev server wired to `tpt-appfront dev --web` CLI command (`tpt-appfront-cli` shells out to `trunk serve --project <dir>`)
- [x] Keyed list diffing for `List`/`DataGrid` — `NodeMeta::key`/`NodeRef::key()` (`crates/tpt-appfront-core/src/ui_tree.rs`) give list items stable identity; `tpt-appfront-dom`'s new `update_list` reconciler (`crates/tpt-appfront-dom/src/lib.rs`) diffs `<ul>`/`<table>` children by key (add/remove/reorder existing DOM nodes) instead of rebuilding the list wholesale on every change. Commit `aeeab85`. Also now drives `VirtualScroll` windowing (renders only the visible-range slice + spacer elements) when `NodeMeta::virtual_scroll` is set — this half is uncommitted in the working tree as of this writing, alongside the `VirtualScroll` primitive itself (Phase 2)
- [x] WASM payload trimming — `tpt-appfront-cli`'s generated `dom/Cargo.toml` template (`crates/tpt-appfront-cli/src/templates.rs::dom_cargo_toml`) now ships `[profile.release]` with `opt-level = "z"`, `lto = true`, `codegen-units = 1`, `panic = "abort"`, `strip = true`; the generated `index.html` sets `data-wasm-opt="z"` on the trunk `<link data-trunk rel="rust">` so `trunk build --release` runs Binaryen `wasm-opt -Oz` on the built module. Leaner allocator not attempted (`wee_alloc` is unmaintained; no drop-in replacement picked yet)
- [x] Fix `reactive_text` effect-cleanup leak — now returns `(Node, EffectHandle)` so the caller owns the lifetime instead of the library always leaking (`crates/tpt-appfront-dom/src/lib.rs`); the one real call site (`crates/tpt-appfront-cli/src/templates.rs`) explicitly `mem::forget`s it since it's a whole-process root mount, now a documented caller choice instead of the default. `mount()` itself still has no `unmount` API
- [~] DOM backend accessibility — `<button>` now gets `type="button"` in both `tpt-appfront-dom` (`render_node`) and `tpt-appfront-html` (SSR) so it never accidentally submits a form; native `<button>`/`<input>`/`<ul>`/`<table>` elements already carry correct implicit ARIA roles and keyboard (Tab/Enter/Space) support from the browser with no extra work needed. Still no explicit `tabindex`/custom keyboard handling for non-native-interactive nodes, since none exist in the current `NodeKind` set
- [~] **Milestone check:** build/serve pipeline verified end-to-end (curl'd the served page, confirmed wasm bundle loads); clicking the button in an actual browser window was not manually driven in this session — no browser automation available here. Recommend a quick manual check: `cd examples/counter-dom && trunk serve`, open http://127.0.0.1:8080, click "+1".

## Phase 4 — Day 4: Canvas Backend (`tpt-appfront-canvas`)
- [x] winit + wgpu + egui window (via `eframe`), `UITree` → egui widgets (`Container`/`List`→plain area, `Heading`/`Text`→painted labels, `Button`→`Button`, `Input`→`TextEdit`, `DataGrid`→flex rows of cells)
- [x] `cosmic-text` for text shaping — wired on native (system font database) and now also on `wasm32`: `crates/tpt-appfront-canvas/fonts/NotoSans-Regular.ttf` (OFL 1.1) is bundled via `include_bytes!` and loaded into `fontdb` at `TextMeasurer::new()` (`crates/tpt-appfront-canvas/src/text.rs`), closing the previous "no bundled font for web" TODO. Still feature-gated behind `full-text-shaping` (off by default → heuristic estimator); a new `TextMeasurer::with_font_data` lets callers swap in a runtime-fetched font instead of the bundled one. `full-text-shaping` also picked up its own `bundled_font_shapes_latin_text` test
- [x] Shared layout via `taffy` (not GPU compute shaders) for v1 — `layout.rs` builds a `taffy` flex tree per frame and `paint.rs` places `egui` widgets at the computed rects
- [x] Runs on both desktop (native) and web (WASM canvas) targets — `cargo check --target wasm32-unknown-unknown` passes; native verified below

### Phase 4b — Lighten `tpt-appfront-canvas` dependency tree (size/startup efficiency)
- [x] Switched `eframe`/`egui` from the default `wgpu` renderer to `glow` (raw GL/GLES) — `crates/tpt-appfront-canvas/Cargo.toml` now sets `eframe = { default-features = false, features = ["default_fonts", "glow", "wayland", "x11"] }`; confirmed `cargo tree -p tpt-appfront-canvas` no longer pulls in `wgpu`/`egui-wgpu`/`pollster` at all, and both native (`cargo build`) and `wasm32-unknown-unknown` build+clippy clean
- [x] Feature-gated `cosmic-text` behind `full-text-shaping` (off by default) — `crates/tpt-appfront-canvas/Cargo.toml` makes the native-only `cosmic-text` dep `optional = true` gated by a new `full-text-shaping` feature; `text.rs`'s `cfg`s updated so the heuristic estimator is now the default `TextMeasurer` on native too (previously it was cosmic-text-always-on for native, heuristic-only for wasm); verified both `cargo build -p tpt-appfront-canvas` (heuristic path) and `cargo build -p tpt-appfront-canvas --features full-text-shaping` (cosmic-text path) compile
- [x] Already covered by the `glow` switch above — `default-features = false` on `eframe` removes unused `wgpu`/persistence features by construction rather than needing a separate audit pass; `winit`'s own default features weren't further pruned (still pulled in transitively at whatever `eframe`'s `glow`/`wayland`/`x11` features require)
- [x] Re-measured binary size before/after (native release build, Windows, `examples/counter-canvas`): checked out the pre-switch commit (`ea8e1e6`) into a scratch worktree, copied `examples/counter-canvas` in and built it there against the `wgpu`-based `tpt-appfront-canvas` for the baseline, then rebuilt the same example at current `HEAD` (`glow`-based). `wgpu` baseline: `counter-canvas.exe` = 15,154,176 bytes (~14.45 MiB). `glow` (current): `counter-canvas.exe` = 7,241,728 bytes (~6.90 MiB). Delta: **-7,912,448 bytes (~-7.55 MiB, ~52% smaller)**

## Phase 5 — Day 5: Compile-Time Optimizer Macro (`tpt-appfront-macros`)
- [x] `#[appfront::component]` proc macro implemented (`tpt-appfront-macros/src/lib.rs`, re-exported as `tpt_appfront_core::component`) — wraps a `UITree`-returning fn and auto-fills `meta.class` (kebab-cased fn name) and `meta.ai.description` (from the doc comment) on the root node if unset
- [x] Static/dynamic detection **and** the Svelte-style compile-time codegen it enables, now both implemented (was previously just the heuristic flag with nothing consuming it):
  - `#[appfront::component]` still uses the token-level heuristic (`tpt-appfront-macros/src/lib.rs::body_reads_signal` scanning for `Signal`/`.get()`/`create_effect`/route-signal markers) since it has no type info at macro-expansion time.
  - `view!` now computes `is_dynamic` **precisely** instead of heuristically: `node_is_static` (`crates/tpt-appfront-macros/src/view.rs`) recursively checks every attribute/text value is a literal (`Expr::Lit`), not a `{ expr }` interpolation. Any provably-static node (and its whole static subtree) is hoisted at codegen time into a call to the new `tpt_appfront_core::static_tree::static_node(id, || ...)` (`crates/tpt-appfront-core/src/static_tree.rs`): a thread-local `HashMap<u64, Rc<dyn Any>>` cache keyed by the address of a per-callsite `static` sentinel, so the subtree is built exactly once and every later render just clones the cached `UITree`. New `ContainerBuilder::new`/`into_only_child`/`with` helpers (`crates/tpt-appfront-core/src/ui_tree.rs`) support extracting/reinserting the cached one-off subtree. A dynamic root can still have individually-cached static children (`parent_static` threading in `gen_node_stmt`/`gen_children`).
  - The flag is now consumed: `tpt-appfront-dom`'s `hydrate_node` (Phase 9) already skipped listener attachment on inert `is_dynamic == false` subtrees; `view!`'s static hoisting now also skips *rebuilding* those subtrees every render, not just skipping hydration work on them.
  - Tests: `crates/tpt-appfront-core/tests/view_macro.rs` (`fully_static_view_is_flagged_static`, `dynamic_view_is_flagged_dynamic`, `static_subtree_inside_dynamic_root_still_builds`, `static_view_is_reproducible_across_calls`) plus unit tests in `static_tree.rs` (build-runs-once, independent ids, cached-clone independence)
- [x] `view!`/`rsx!` templating macro — `crates/tpt-appfront-macros/src/view.rs` implements `view!` (re-exported as `tpt_appfront_core::view`), an HTML-like template macro covering `Container`/`Heading`/`Text`/`Button`/`Input` with token-span error reporting for unknown tags, missing required attributes (`level`/`on_click`/`value`), and child-shape mistakes. It expands to the same `ContainerBuilder` chain you'd hand-write, so there's no hidden runtime cost or separate AST. Tests in `crates/tpt-appfront-core/tests/view_macro.rs`. v1 does not cover `List`/`DataGrid` or two-way binding, and isn't a full Leptos/Dioxus replacement by design

## Phase 6 — Day 6: AI Schema Backend (`tpt-appfront-ai-schema` + `tpt-appfront-html`)
- [x] Freeze format first: `docs/ai-schema.md` (JSON-LD shape + custom AI Schema shape)
- [x] `UITree` → JSON-LD (structured data / rich snippets)
- [x] `UITree` → custom AI Schema (interactive elements, actions, params)
- [x] Fix per-request panic risk in `tpt-appfront-ai-schema/src/ai_schema.rs` — `to_ai_schema_value` now returns `Result<Value, serde_json::Error>` instead of `.expect(...)`-panicking; note `tpt-appfront-server`'s `ai_agent_json` handler doesn't call this fn directly (it serializes via `serde_json::json!`/axum's `Json` extractor, which already can't panic the process on failure)
- [x] `tpt-appfront-html`: `UITree` → semantic HTML string (SSR/SSG), including `data-ai-action`/OpenGraph tags

## Phase 7 — Day 7: Smart Router
- [x] `tpt-appfront-server` crate: Axum server with `ClientKind` detection (User-Agent/query param) → human (WASM shell), crawler (HTML via `tpt-appfront-html`), AI agent (JSON-LD + AI Schema via `tpt-appfront-ai-schema`), social bot (OpenGraph via `tpt-appfront-html`)
- [x] `SmartRouter` / `SmartRouterBuilder` for configuring the server (static dir, wasm path, title, description)
- [x] `tpt-appfront dev --web` and `tpt-appfront build --target <target>` wired into CLI (as part of broader `init`/`dev`/`build` commands)
- [x] Fixed pre-existing `serde_json::Map` indexing bug in `tpt-appfront-ai-schema/src/json_ld.rs` (s/`map["key"] = val`/`map.insert("key", val)`/)
- [x] Fixed legacy WASM-shell template bug: positional `{}` placeholders collided if `title`/`description` contained literal braces — switched to named `{TITLE}`/`{DESC}`/`{WASM}` placeholders; `build_router` also now exported from `tpt-appfront-server` for standalone `axum::serve` use (needed by e2e tests)
- [x] Production hardening middleware for `tpt-appfront-server` — `build_router` now layers `TraceLayer::new_for_http()`, `TimeoutLayer::new(Duration::from_secs(10))`, `SetResponseHeaderLayer` for `X-Content-Type-Options: nosniff` / `X-Frame-Options: DENY` / a same-origin `Content-Security-Policy`, and an explicit `CorsLayer::permissive()` (`crates/tpt-appfront-server/src/router.rs`); `tower-http` now enables `trace`/`timeout`/`set-header`/`cors` features. `CorsLayer::permissive()` is still a wide-open policy (mirrors current no-CORS-at-all behavior) — tightening to an allowlist is left for when real cross-origin clients exist. No `tracing-subscriber` init added since that's an application/binary concern, not this library's

## Phase 8 — CLI & Project Scaffold (`tpt-appfront-cli`, `clap`)
- [x] `tpt-appfront init <name>` — scaffolds `canvas/`+`dom/` (or a single crate via `--target canvas|dom`), path deps resolved from the CLI's own `CARGO_MANIFEST_DIR`; verified `cargo check` (native) and `cargo check --target wasm32-unknown-unknown` both pass on the generated crates with zero edits
- [x] `tpt-appfront dev --desktop` (`cargo run` in `--project <dir>`, native winit/egui window); now has a watch/reload loop by default (polls `src/**/*.rs` + `Cargo.toml`/`Cargo.lock`, debounces, and restarts the `cargo run` child on change) — `--no-reload` restores the plain single `cargo run` (`crates/tpt-appfront-cli/src/main.rs`)
- [x] `tpt-appfront build --target <target>` — `canvas`/`dom` shell out to real `cargo build --release` / `trunk build --release`; `html`/`ai-schema` print embedding guidance since they're libraries, not standalone artifacts
- [x] `examples/` directory (DOM counter, Canvas counter, minimal SSR page) built in CI — `examples/counter-dom`, `examples/counter-canvas`, `examples/ssr-page` (renders a `UITree` to a semantic HTML string via `tpt-appfront-html` and prints it) all present and each has a committed `Cargo.lock`. Commit `aeeab85`
- [x] AI-agent demo example — `examples/ai-agent-demo/src/main.rs` showcases `query_state()`/`navigate_to()`/`trigger_event()` (Phase 10) driving a `UITree` headlessly, simulating an LLM agent inspecting `AgentState`/`ElementSummary` JSON and dispatching actions without a browser. Commit `aeeab85`
- [x] `docs/quickstart.md`: install → init → dev → build

## Phase 9 — Streaming Hydration (Resumability)
- [x] Server renders HTML + serializes state (`serde_json`) into the page
- [x] Client (WASM) resumes: attaches listeners to existing DOM nodes instead of re-rendering
- [x] Islands / partial hydration — only hydrate interactive subtrees (buttons/inputs), leave static content (headings, text) as inert HTML to cut WASM payload/hydration cost for content-heavy pages. Implemented in `tpt-appfront-dom`'s `hydrate_node` (`crates/tpt-appfront-dom/src/lib.rs`): a bottom-up pass attaches listeners only to nodes whose subtree has `on_click`/`ai.action` or is flagged `is_dynamic`, skipping provably-inert static content. `reconcile_keys` (`crates/tpt-appfront-core/src/reconcile.rs`) adds a native-testable, backend-agnostic keyed-diff primitive (used conceptually by the DOM `update_list` reconciler, which is `wasm32`-only and can't run native tests)

## Phase 10 — Programmatic AI Agent API
- [x] `appfront::query_state()`, `appfront::navigate_to()`, `appfront::trigger_event()` headless API
- [x] Reactive route signal (`route_signal()`, `current_route()`) for effects to subscribe to
- [x] `AgentState` / `ElementSummary` structs serializable as JSON for LLM consumption
- [x] UITree/signal devtools inspector — `crates/tpt-appfront-core/src/devtools.rs` reuses `AgentState`/`ElementSummary` plus `signal_activity` (signals named via `Signal::labeled`) to render a plain-text/human tree view (`inspect_tree`), an agent-state listing (`inspect_state`), and a combined `render`/`to_html` report. Lets a developer inspect the tree, the interactive/data elements an AI agent sees, and which labeled signals have been firing — no browser needed. Tests cover structure, state listing, full report, HTML escaping, and labeled-signal activity
- [x] MCP server exposing the agent API — new crate `tpt-appfront-mcp` (`crates/tpt-appfront-mcp/src/lib.rs`, added to the workspace `Cargo.toml`): `McpServer<Msg>` auto-generates one MCP tool per interactive `UITree` node's `AiMeta` (built-in `query_state`/`navigate` tools plus one tool per node action), wired to `tpt_appfront_core::query_state`/`navigate_to` and an app-supplied `on_command` closure (mirroring `tpt-appfront-server`'s `POST /command` `Command`/`CommandResponse` shape). Speaks JSON-RPC 2.0 newline-delimited over stdio (no async runtime), the standard local-MCP-server transport. Highest-leverage differentiator identified so far, now implemented. *(uncommitted in working tree as of this writing — no example wiring it up yet, and no dedicated test file)*
- [x] Bidirectional AI command endpoint — `crates/tpt-appfront-server/src/router.rs` now has `POST /command`: a `Command { action: String, params: HashMap<String, serde_json::Value> }` (`serde::Deserialize`) is read via axum's `Json` extractor (rejects malformed JSON with 400 for free), validated (empty `action` → 400; not in `SmartRouterBuilder::allowed_actions` allowlist, if configured → 403), then handed to an app-supplied `on_command(Command) -> CommandResponse` closure (501 if none configured) — the closure is where an app calls its own `trigger_event`/`navigate_to` against its actual `Msg`/state, since the router is generic over `Msg` and has no live app state of its own to dispatch into. Route is body-size-limited to 16KiB via `DefaultBodyLimit` (closes the Phase 12 note below about not retrofitting this after the fact). 6 new tests in `router.rs` cover: no-handler 501, empty-action 400, allowlist-rejected 403, successful dispatch + response body, oversized-body 413. MCP-server auto-wiring (translating `AiMeta` into MCP tool definitions) is still not built — this endpoint is the manual-`Command` layer underneath it

## Phase 13 — Terminal/TUI Backend (`tpt-appfront-tui`) — proves the "one AST, N renderers" thesis further
Positioning: `tpt-appfront-core` already has no opinion on rendering (`tpt-appfront-html`, `tpt-appfront-canvas`, `tpt-appfront-ai-schema` are all independent interpreters of the same `UITree`). A `ratatui`-based backend would let one codebase ship as a web app, a native window, *and* a terminal app — a claim no DOM-rooted framework (Leptos/Dioxus/Yew) can make without a rewrite of their AST.
- [x] New crate `tpt-appfront-tui`: `NodeKind` → `ratatui` widget mapping (`Container`→vertical layout split, `Text`/`Heading`→paragraph, `Button`→bracketed focusable paragraph, `Input`→editable line, `List`→`ratatui` list, `DataGrid`→`ratatui` table), mirroring how `tpt-appfront-canvas/src/paint.rs` maps `UITree`→`egui`. Rendering is pure and headless-testable via `ratatui`'s `TestBackend` (`render`/`render_to_buffer`/`buffer_to_string`). Added to the workspace `Cargo.toml`, deps: `ratatui` 0.30 + `crossterm` 0.28
- [x] Keyboard-driven event dispatch (Tab/↑←/↓→ move focus, Enter/Space activate the focused button, Esc quits, typing edits the focused `Input`) back through the same `Msg` dispatch pattern used by `tpt-appfront-dom`/`tpt-appfront-canvas` — implemented as a unit-testable `TuiDriver::on_key` (no TTY needed) plus a `run` loop over crossterm events
- [x] `tpt-appfront dev --tui` / `tpt-appfront build --target tui` CLI wiring, parallel to existing `--target canvas`/`dom` (`InitTarget::Tui` scaffolds a `tui` crate via new `tui_cargo_toml`/`tui_main_rs` templates; `dev --tui` and `build --target tui|terminal` shell out to `cargo run`/`cargo build --release`)
- [x] `examples/counter-tui` — same counter `UITree` as the other examples; builds clean (`cargo build` + `cargo clippy -D warnings`) and is now covered by the CI `examples` job's native-examples step. Verified headlessly via `tpt-appfront-tui`'s `TestBackend` snapshot + keyboard-driver unit tests; an actual interactive terminal run was not driven in this session (no TTY in the sandbox) — recommend `cd examples/counter-tui && cargo run`, Tab to focus `+1`, Enter to increment, Esc to quit

## Phase 12 — Webview Desktop Shell (`tpt-appfront-webview`) — primary "beat Tauri/Electron" target
Positioning: matches Tauri on binary size/startup (both wrap the OS's own webview — WebView2/WKWebView/WebKitGTK — instead of Electron's bundled Chromium+Node), but needs no second JS/npm toolchain at all, since `tpt-appfront-dom`/`tpt-appfront-html` already render the UI in pure Rust. `tpt-appfront-canvas` remains the opt-in path for GPU-bound apps (data viz, editors, games), not the default desktop story.
- [x] New crate `tpt-appfront-webview`: thin native window via `wry` + `tao`, no bundled renderer (`crates/tpt-appfront-webview/src/lib.rs`); the `run(WebviewOptions, on_command)` entry point opens the window and serves a `trunk`-built `dist/` directory over an `app://` custom protocol
- [x] Host the existing `tpt-appfront-dom` WASM bundle (built via `trunk`) inside the webview for interactive apps, reusing Phase 3/9 hydration work as-is — the custom protocol serves `index.html` + assets straight from the `dist/` dir, so the same DOM app runs unchanged
- [x] Native <-> webview IPC: dispatch `Msg` from JS/DOM events back into Rust app state (reuse `serde_json` state serialization already used for hydration, Phase 9) — `tpt-appfront-dom` already emits `data-ai-action` on interactive nodes; an injected init script auto-posts `{action, params}` over the webview IPC channel on click, and `window.__appfront.post(...)` is exposed for custom JS
- [x] `tpt-appfront dev --desktop-webview` / `tpt-appfront build --target webview` CLI wiring (`tpt-appfront-cli`), parallel to existing `--target canvas`/`dom` (builds the `ui/` trunk sub-app, then `cargo run`/`cargo build --release` on the host)
- [x] `examples/counter-webview` — host crate (`src/main.rs`, `tpt_appfront_webview::run` with a `["increment"]` IPC allowlist + 20 cmd/s cap) + nested `ui/` `tpt-appfront-dom` counter app, built via the CLI (window opens, click increments, no Node/npm install required anywhere in the build). Runtime/visual check (window opens, click increments) is **manual** — this sandbox has no display/WebView2 runtime; verified to compile (`cargo build` of the host) and clippy-clean. `.github/workflows/ci.yml`'s `examples` job now explicitly detects and skips any example with a `ui/index.html` subdirectory (needs system webview libs + a pre-built `ui/dist` that CI doesn't produce), logging why instead of silently building nothing/erroring. **Correction (2026-07-20):** this claim was stale/false — `src/main.rs` referenced an undefined `appfront_webview::` crate path and never imported `AppBuilder`, and `tpt-appfront-webview` itself was missing 10 dependencies (see Phase 17) — none of it actually compiled. Fixed in Phase 17; the host crate builds clean again as of that pass. **Correction (2026-07-21):** the "nested `ui/` app still doesn't build" claim is now stale too — `cargo build --target wasm32-unknown-unknown` in `examples/counter-webview/ui` and `cargo build` in `examples/counter-webview` both succeed cleanly, and `tpt-appfront-webview` is clippy-clean (`-D warnings`). Still unverified: an actual `trunk build` of `ui/` into `dist/` and a real window-open/click smoke test (no `trunk`, display, or WebView2 runtime in this sandbox)
- [ ] Binary size comparison recorded here against a minimal Tauri + Electron equivalent of the same counter app — not measured in this environment (no release baseline captured); defer to a local release build on the target OS
- [x] IPC capability allowlisting — `WebviewOptions::allowed_actions` is an explicit allowlist of action strings a hosted page may dispatch back to native; anything not on the list is rejected (logged, ignored), avoiding the Electron-style open-bridge vulnerability
- [x] Request body-size limit landed alongside `POST /command` (Phase 10: `DefaultBodyLimit::max(16 * 1024)` scoped to that route in `crates/tpt-appfront-server/src/router.rs`) instead of being retrofitted later. Rate limiting now added on both sides: `tpt-appfront-server`'s `POST /command` is layered with `tower_governor::GovernorLayer` (`SmartRouterBuilder::rate_limit`, default 5 req/s with a burst of 10), and `tpt-appfront-webview`'s local IPC bridge (`handle_ipc` in `crates/tpt-appfront-webview/src/lib.rs`) checks a `governor::RateLimiter` before dispatching to `on_command` (`WebviewOptions::max_commands_per_second`). Known simplification: the server-side limiter uses `GlobalKeyExtractor` (one shared bucket for the route, not per-client) since this router doesn't plumb `ConnectInfo<SocketAddr>`/`into_make_service_with_connect_info` anywhere yet — per-IP limiting is a follow-up once that's wired up
- [ ] Stretch: mobile via the same webview shell — `wry`/`tao` have partial iOS/Android support, extend `tpt-appfront-webview` there later

## Phase 11 — Stretch / Optional
- [ ] GPU-accelerated layout via WebGPU compute shaders (feature-flagged; `taffy` remains default)
  - [x] Runtime `AutoOptimizer` (frame-time profiling → auto-toggle texture caching/virtual scrolling) — `crates/tpt-appfront-canvas/src/auto_optimizer.rs`: pure/headless `AutoOptimizer` records per-frame ms (EMA + hysteresis) and recommends toggling `virtual_scrolling`/`texture_caching`; `CanvasApp` now owns one and calls `record_frame` each `ui()` (`frame_ms()`/`optimizer_state()` accessors). Logic unit-tested; canvas doesn't yet *consume* the recommendations to change rendering (no virtual-scroll/texture-cache path exists in Phase 4 yet), so this is the profiling+decision half wired, not the applied half
- [~] `tpt-appfront generate --prompt "..."` UI scaffolding — implemented as an **offline, rule-based** generator, not a live LLM call (no API key/network dependency): `crates/tpt-appfront-cli/src/generate.rs` keyword-matches the prompt against 5 known patterns (counter, login form, todo list, card, nav bar) and emits the corresponding `view!` snippet, falling back to a minimal skeleton + a hint on recognized keywords when nothing matches. Wired as `tpt-appfront generate --prompt "<text>" [--out <file>]` (`crates/tpt-appfront-cli/src/main.rs`). Every emitted snippet is proven to actually compile against the real `view!` macro (not just visually plausible) via `crates/tpt-appfront-core/tests/generate_snippets_compile.rs`, which instantiates all 6 snippets (5 patterns + fallback) against a matching `Msg` enum. A real `--llm` mode that calls out to a model provider for open-ended prompts remains future work
  - [x] `tpt-appfront benchmark` / `tpt-appfront optimize --auto` CI pipeline commands — `tpt-appfront-cli` now has `benchmark` (`cargo bench` in the project) and `optimize` (`--target canvas|dom|webview|all`, `--auto` size-optimized profile, reports the largest release artifact size in MiB) subcommands, plus a `--bundle` flag on `build`/`optimize` that writes a `packager.toml` (if missing) and shells out to `cargo packager`. Parse + template tests added. These shell out to tools (`cargo bench`/`cargo packager`) that may not be installed in every environment, so the commands are thin wrappers, not self-contained builders
- [x] Styling macro layer (Tailwind-like `.class("bg-blue-500 p-4")` wrapper) — `crates/tpt-appfront-core/src/styling.rs`: a dependency-free, curated `UTILITIES: &[(&str, &str)]` dictionary (spacing/color/typography/layout/flex) resolved at runtime, no external build step/scanner. `lookup`/`is_utility`/`class_value`/`inline_style`/`style_sheet` cover the SSR-inline-style, shipped-`<style>`-block, and `af-u-`-prefixed-class-value cases respectively. The companion `class!` macro (`crates/tpt-appfront-core/src/lib.rs`, `#[macro_export]`) gives `.class(class!("bg-blue-500", "p-4"))` a **compile-time** unknown-utility check (`styling::class_macro_check`, a `const fn` since `&str::eq` isn't const-stable — hand-rolled `str_eq`). Wired into SSR: `tpt-appfront-html`'s `attrs()` now emits a `style="..."` attribute from `styling::inline_style` alongside `class` (`utility_classes_emit_inline_style`/`unknown_class_not_styled` tests). Not yet consumed by `tpt-appfront-dom`/`tpt-appfront-canvas` — DOM apps would need to embed `styling::style_sheet(...)` in their page `<head>` manually; canvas doesn't read utility classes for painting at all
- [x] PWA/offline service worker — new `crates/tpt-appfront-server/src/pwa.rs`: `PwaConfig` (name/short_name/description/theme+background color/start_url/cache_name/precache list) drives `service_worker()` (generates an offline-first, cache-first-with-network-fallback-and-refresh `service-worker.js`), `manifest()` (JSON `manifest.webmanifest`), and `manifest_link()`/`registration_script()` HTML glue. Wired into `SmartRouter`/`SmartRouterBuilder::pwa(PwaConfig)` (`crates/tpt-appfront-server/src/router.rs`): serves `GET /service-worker.js` and `GET /manifest.webmanifest`, and `inject_pwa` splices the manifest `<link>`/registration `<script>` into both the WASM-shell and hydration-shell HTML when configured. Tests: `pwa_routes_served_and_shell_injects_glue`, `hydration_shell_injects_pwa_glue`, plus `pwa.rs`'s own unit tests for precache/fallback/manifest-JSON-validity
  - [x] Packaging/distribution: reuse `cargo-packager` (or vendor `tauri-bundler`) for `tpt-appfront build --target webview|canvas` to produce signed installers (.msi/.dmg/.AppImage/.deb) + delta auto-update, instead of the current raw `cargo build --release` — `tpt-appfront build --bundle` / `tpt-appfront optimize --bundle` write a `packager.toml` (template in `crates/tpt-appfront-cli/src/templates.rs::packager_toml`, per-OS formats + `generate-updates = true`) and run `cargo packager`. Closes the biggest remaining Tauri DX gap at the CLI level; actual signing keys / notarization still need real `cargo-packager` config + secrets in CI
- [x] Accessibility: wire `AccessKit` into `tpt-appfront-canvas` painting so GPU-canvas apps aren't a screen-reader dead-end (webview shell already gets this for free from the OS webview) — new opt-in `accesskit` feature (`crates/tpt-appfront-canvas/Cargo.toml`: `eframe/accesskit` + `egui/accesskit`, off by default to keep the minimal dependency surface from Phase 4b). `paint.rs` (`#[cfg(feature = "accesskit")]`-gated): `Button`/`Input` get `response.set_accessible_name`/`set_accessible_description` from the widget label/`meta.ai.description`; `Heading`/`Text` (which egui's `painter().text` doesn't register with AccessKit on its own) get a new `name_accessible_node` helper that registers a non-focusable (`Sense::hover()`) named node with the correct role so screen readers see painted-only content too
- [x] Signal core: computed/memo signals + batched `set()` (dedupe diamond-dependency re-runs) in `tpt-appfront-core::signal` — done, see Phase 1
- [x] Extend `tpt-appfront-dom` to batch DOM mutations into a single `requestAnimationFrame` callback per frame instead of updating synchronously per `set()` (`reactive_text` in `crates/tpt-appfront-dom/src/lib.rs` issues one direct `web_sys` call per signal change) — already done: `schedule_text_update`/`flush_text_updates` coalesce all `reactive_text` node updates into one `requestAnimationFrame` flush per frame (deduping same-frame writes), cutting redundant WASM/JS boundary crossings when several signals change together

## Phase 14 — Adoption & Hardening Follow-ups (in progress)
Tracking the items from the 2026-07-12 adoption/improvement review (see repo conversation history — no separate doc committed for the review itself).
- [x] `tpt-appfront-server`: replaced `CorsLayer::permissive()` on the read routes with a configurable `CorsPolicy` (`SmartRouterBuilder::cors(...)`, defaults to `Permissive` — unchanged behavior), and switched `POST /command`'s rate limiter from `GlobalKeyExtractor` to `PeerIpKeyExtractor` (`crates/tpt-appfront-server/src/router.rs`). `serve()` now serves via `into_make_service_with_connect_info::<SocketAddr>()` so `ConnectInfo` is populated; `build_router`'s docs flag this requirement for callers wiring their own `axum::serve`. `e2e_test.rs`'s `ServerGuard` updated to the same pattern. 3 new tests (`command_rate_limit_is_per_peer_ip`, `default_cors_is_permissive`, `cors_origins_policy_restricts_allowed_origin_header`); existing `/command` tests updated to attach a `ConnectInfo` extension via a new `test_connect_info()`/`connect_info_for()` test helper. `cargo test -p tpt-appfront-server` (35 tests) and `cargo clippy -p tpt-appfront-server --all-targets -- -D warnings` both pass
- [x] `view!`/`rsx!` macro (`tpt-appfront-macros/src/view.rs`): `List`/`DataGrid` tag support and two-way binding added — `TAGS` now includes `List`/`DataGrid` (self-closing, `columns`/`rows` as `{ expr }`s for `DataGrid`, children build onto the inner list for `List`), and `<Input>` gained an `on_input={Fn(String) -> Msg}` attribute wired to `.on_input(..)` for two-way binding. Tests in `crates/tpt-appfront-core/tests/view_macro.rs` (`list_tag_builds_items_as_children`, `data_grid_tag_builds_columns_and_rows`, `data_grid_rejects_children`, `two_way_binding_emits_on_input`)
- [x] Added a richer example beyond the 6 counter apps — `examples/todo-app` (DOM backend): a todo list built with `view!`'s new `List`/`DataGrid`/two-way-binding support (`Input` bound via `on_input={Msg::SetDraft}`, a `List` of quick actions, a `DataGrid` for the todo table). `cargo check --target wasm32-unknown-unknown` passes. Committed; picked up automatically by the CI `examples` job's `examples/*/` glob (has its own `index.html`) and by the root `Cargo.toml`'s blanket `exclude = ["examples"]`, so no extra CI wiring was needed. Still not manually driven in a real browser via `trunk serve` this session
- [x] Hot-reload for `tpt-appfront dev --desktop` (was a plain `cargo run`, no watch/reload loop since Phase 8) — now implemented as a dependency-free poll-based watcher in `crates/tpt-appfront-cli/src/main.rs`: `dev --desktop` spawns `cargo run`, watches `src/**/*.rs` + `Cargo.toml`/`Cargo.lock`, and restarts the child process on a debounced change. `--no-reload` opts back to a plain single `cargo run`. Compile errors don't abort the loop (the failing child exits, the next save retries). `cargo test -p tpt-appfront-cli` + clippy clean
- [ ] Binary-size-vs-Tauri comparison for `tpt-appfront-webview` (Phase 12 item, still unmeasured) — needs a local machine with the Tauri/Node toolchain installed; not measurable in this sandbox

## Phase 15 — 2026-07-13 Platform Review Follow-ups
Tracking items from a 2026-07-13 platform-wide review (bugs, gaps, and adoption/innovation suggestions — see repo conversation history, no separate doc committed).
- [x] **Fix `tpt-appfront init` path-dependency resolution for published installs** — `dep_path()`/`crates_dir()` (`crates/tpt-appfront-cli/src/main.rs:179-181`) now go through `dep_ref()`/`is_workspace_checkout()`/`published_version()`: when the sibling `crates/tpt-appfront-core/Cargo.toml` exists (monorepo checkout) scaffolded `Cargo.toml`s use `path` deps; otherwise they use a version dep (this CLI's `CARGO_PKG_VERSION`, overridable via `TPT_APPFRONT_DEP_VERSION`). The `doctor` command reports which mode it's running in (`crates/tpt-appfront-cli/src/main.rs:745`)
- [x] Cap `tpt-appfront-webview` IPC message size before parsing — `handle_ipc` (`crates/tpt-appfront-webview/src/lib.rs:442`) rejects any message longer than `MAX_IPC_MESSAGE_BYTES` (16 KiB, mirroring `POST /command`) *before* `serde_json::from_str` runs, with a dedicated test `handle_ipc_rejects_oversized_messages_before_parsing`
- [x] Document/fix `tpt-appfront-dom`'s unmount API — `MountedRoot` (`crates/tpt-appfront-dom/src/lib.rs:42`) now owns every tracked closure and `EffectHandle`; `MountedRoot::unmount` drops them, and `LIVE_CLOSURES` bookkeeping + `drop_closures_for`/`track_closure` mean swapping/removing a subtree (route change, modal close) no longer leaks listeners. Whole-process SPA root mounts still opt into `mem::forget` explicitly
- [x] Wire `VirtualScroll` into `tpt-appfront-canvas` — `layout.rs` `build_virtual_list`/`build_virtual_data_grid` window `List`/`DataGrid` items to the visible range plus top/bottom spacer leaves, so a list/grid with thousands of rows lays out and paints only the handful in view (matching the DOM backend), not every row every frame
- [x] Give `tpt-appfront-canvas` a styling adapter for `styling::class!`/utility classes — `canvas_style_for` (`crates/tpt-appfront-canvas/src/layout.rs:582`) maps padding (`p-*`,`px-*`,`py-*`)/font-size (`text-xs`..`text-2xl`)/color (`bg-*`/`text-*`) utilities into `taffy` layout + `egui` paint; `paint.rs` applies `background`/`foreground` so `class="..."` is no longer a silent no-op on canvas. Accepts both bare and `af-u-` prefixed utility names
- [x] Add `{if}`/`{for}` control flow and component-tag composition to `view!` (`crates/tpt-appfront-macros/src/view.rs`) — `{if cond {..} else {..}}`/`{for pat in iter {..}}` blocks produce zero-or-more children; a `{ expr }` that yields a `UITree<Msg>` composes a sub-view/component. Combined with the existing `List`/`DataGrid`/two-way-binding support, `view!` covers the common DX gap vs. Leptos/Dioxus/Yew
- [x] `tpt-appfront doctor` command — pre-flight check for `trunk`, `wasm32-unknown-unknown` target, `cargo-packager`, and the path-dep vs published-install mode; runs before scaffolding/building instead of surfacing as a raw cargo error mid-build (`crates/tpt-appfront-cli/src/main.rs:745`)
- [x] `tpt-appfront add component <name>` / `tpt-appfront add page <name>` — `add` subcommand (`crates/tpt-appfront-cli/src/main.rs`) scaffolds a `view!`-based reusable component under `src/components/<name>.rs` or a route-sized page under `src/pages/<name>.rs`, and best-effort appends the `mod` declaration to the project's root source file. Rejects non-identifier names and duplicate files
- [x] Real browser/webview end-to-end tests — `crates/tpt-appfront-dom` has `wasm-bindgen-test` browser tests (mount/hydrate/click) run headlessly via `wasm-bindgen-test-runner` in the CI `wasm-tests` job (`cargo test -p tpt-appfront-dom --target wasm32-unknown-unknown`); the `mcp` crate has stdio JSON-RPC e2e tests. (A real `trunk serve` + Playwright click still isn't automated — no browser driver in CI — but the in-browser `wasm-bindgen-test` path now exists where none did)
- [x] Commit `examples/node-graph/` — added a `README.md` and rewrote `src/main.rs` as a documented raw-`egui`/`eframe` escape-hatch (pan/zoom/absolute world-space/drag that `tpt-appfront-canvas`'s taffy flexbox can't express), proving the recommended pattern for GPU/canvas-style UIs. Builds clean (`cargo build` native, `cargo clippy --all-targets -- -D warnings`) against egui 0.35 after fixing the `App::ui`/`smooth_scroll_delta`/`StrokeKind` API changes
- [ ] Stretch: live LLM-backed `tpt-appfront generate --llm` — today's `generate.rs` is offline/keyword-matched only (documented in Phase 11); a real LLM-backed mode would be a natural differentiator given the existing AI-schema/MCP investment.

## Phase 16 — GUI Templates & Migration Ingest (planned)
Onboarding gap: today the *only* starter UI a new developer sees, via `tpt-appfront init` or any `examples/counter-*`, is a plain counter — there's no example of a "real" UI shape (a form, a nav+content layout, a CRUD list), despite the `UITree<Msg>`/multi-backend model being unfamiliar territory coming from typical UI frameworks. Plan: `C:\Users\phill\.claude\plans\i-was-thinking-of-mutable-abelson.md`.
- [x] New crate `tpt-appfront-templates` (depends only on `tpt-appfront-core`, so it stays usable from canvas/dom/tui/html consumers alike): 3 backend-agnostic starter templates as stateless builder functions `(config with current values + Msg callbacks) -> UITree<Msg>` — `login_form`, `dashboard_shell` (nav + content area), `settings_list` (CRUD list, using `.list()` of per-row containers rather than `DataGrid` so Edit/Delete buttons have somewhere to live)
- [x] Companion runnable example `examples/templates-demo` (modeled on `examples/todo-app`) wiring all 3 templates into one DOM app, proving composition (`dashboard_shell`'s `content` filled with `settings_list`'s output) actually works end to end — builds via `trunk build` (wasm) in the CI `examples` job
- [x] `tpt-appfront ingest <input.html> [--out <file>]` CLI subcommand (`crates/tpt-appfront-cli/src/ingest.rs`, `scraper` dependency scoped to the CLI crate only): converts existing static/server-rendered HTML into a `view!` builder-call skeleton — structure only (tags/classes), never silently drops behavior: inline event handlers are emitted as `todo!()` stubs instead of guessed `Msg` values, and a summary of anything unmapped/dropped is printed to stderr
- [x] Follow-up once the above is validated: `generate.rs` keyword patterns now point at the prebuilt `tpt_appfront_templates::settings_list`/`dashboard_shell` for the `settings`/`dashboard` prompts (with inline `view!` equivalents), and `templates-demo` exercises composition end to end
- [x] Doc fix (unrelated, noticed along the way): CLAUDE.md's architecture section now says `view!` supports `List`/`DataGrid`/two-way binding/`{if}`/`{for}`/component-tag composition — matches the implementation as of Phase 14/15

## Phase 17 — 2026-07-20 Dependency Review & Build-Breakage Follow-ups
Triggered by a plain "review the dependencies" ask that turned into discovering the workspace didn't actually build clean in several places — see repo conversation history, no separate doc committed.
- [x] Dependency hygiene: removed unused `tpt-appfront-core` dep from `tpt-appfront-webview/Cargo.toml` (never referenced in its `src/`), removed unused `serde` dep from `tpt-appfront-mcp/Cargo.toml` (only `serde_json` is actually used), bumped `tpt-appfront-tui`'s `crossterm` from `"0.28"` to `"0.29"` to collapse a duplicate resolved version already pulled in transitively via `ratatui 0.30`
- [x] **`tpt-appfront-webview` didn't compile at all** — `arboard`, `dirs`, `notify-rust`, `rfd`, `keyring`, `global-hotkey`, `crossbeam-channel`, `fs4`, and (Windows-only) `winreg`/`windows-sys` were all used in `src/*.rs` (clipboard/dialog/notify/secret/shortcut/single_instance/tray/deeplink/window_state) but never declared as dependencies. Added all of them to `Cargo.toml` (`tray` gated as an optional feature, matching the existing `#[cfg(feature = "tray")]` code). Also fixed a duplicate `MAX_IPC_MESSAGE_BYTES` const and a `handle_ipc` signature that took a stale `allowed: &HashSet<String>` param instead of the `acl: &Acl` its body already used, and updated a `global_hotkey::HotKey` test that touched now-private `mods`/`key` fields to use the public `.matches(..)` API instead. All 12 crate tests pass; verified against unmodified `master` via `git stash` that none of this was caused by the dependency edits themselves
- [x] **`tpt-appfront-server`'s ETag/`If-None-Match` caching was dead code** — `router/caching.rs`'s `cached_html`/`cached_json` (backed by `OnceLock` cells already sitting unused on `SmartRouter`) were never called; `crawler_html`/`social_opengraph`/`ai_agent_json` in `router/handlers.rs` took the wrong signatures (no `headers`, returned `Html<String>`/`Json<Value>` instead of `Response`) even though their own call sites already passed `headers` and expected a `Response`. Wired the three handlers through the caching helpers (added a new `opengraph_cache` field alongside the existing `html_cache`/`ai_schema_cache`); all 43 tests pass including the previously-broken `cached_read_route_honors_if_none_match`
- [x] Leftover pre-`tpt-` crate-rename references (`appfront_core::` etc. instead of `tpt_appfront_core::`) fixed across `tpt-appfront-html`, `tpt-appfront-dom`, `tpt-appfront-macros`, `tpt-appfront-canvas`, `tpt-appfront-core`'s own tests, and `examples/counter-webview/src/main.rs` (which also had an unimported `AppBuilder`)
- [x] `tpt-appfront-core`'s wasm32 `WebStorage` (`src/store.rs`) called `web_sys::window()` without `web-sys` ever being declared as a dependency — added it as a `cfg(target_arch = "wasm32")` dependency with `Window`/`Storage` features
- [x] `tpt-appfront-dom` was missing `Router`/`create_effect`/`KeyedDiff`/`reconcile_keys` from its `use tpt_appfront_core::{...}` import list, and called an entirely undefined `drop_closures_for` function (paired with the existing `track_closure`/`LIVE_CLOSURES` bookkeeping but never implemented) — added the missing imports and the function
- [x] **`tpt-appfront-dom` now implements the `Textarea`/`Checkbox`/`Select`/`Radio` `NodeKind` variants** — `kind_matches_dom`, `render_node`, and `hydrate_node` all match these kinds, mirroring the HTML backend's markup; `NodeMeta::on_input` is wired to `HtmlTextAreaElement`/`HtmlSelectElement` and `NodeMeta::on_toggle` to the checkbox `change` event. Verified: `cargo build -p tpt-appfront-dom --target wasm32-unknown-unknown` is green and `crates/tpt-appfront-dom/tests/form_controls.rs` passes (this had been a blocker flagged earlier; resolved)

## Phase 18 — 2026-07-20 Review Follow-ups (bugs + adoption)
Triggered by a "review the platform for bugs/todos/missing features/adoption" ask; three parallel surveys (todo.md/spec.txt status, docs/examples/CLI onboarding, code scan of recently-touched/security-sensitive paths) turned up a live correctness bug plus onboarding gaps.
- [x] Fix DataGrid virtual-scroll paint bug: `paint_data_grid` (`crates/tpt-appfront-canvas/src/paint.rs`) derived which row's text to paint from taffy child position (`row_idx - 1`), which breaks once a virtualized grid's spacer rows shift positions — wrong text silently painted, or an out-of-bounds panic once scrolled past the first window. `layout.rs`'s `RenderNode::grid_cells` now carries a `GridRowKind` (`Header`/`Spacer`/`Data(idx)`) per row (`build_data_grid`/`build_virtual_data_grid` both updated, plus a `virtual_data_grid_maps_rows_to_correct_source_index_after_scroll` test) so `paint.rs` can look up the real source row instead of inferring it; `paint.rs`'s `paint_data_grid` consumes `GridRowKind::Data(source_idx)` directly (`match grid_row.kind { Header => columns, Spacer => continue, Data(idx) => &rows[idx] }`), confirmed by the existing headless paint test
- [x] Commit and wire the in-progress `tpt-appfront-templates` crate + `ingest.rs` CLI subcommand + `examples/templates-demo` into `README.md`'s example list and `docs/quickstart.md` — `README.md` now lists `tpt-appfront-templates` in workspace layout and `templates-demo`/`node-graph` in the examples line; `docs/quickstart.md` gained a "Starter templates & migration" section covering `generate`/`ingest`/`add component`/`add page`, and fixed a stale `crates/appfront-*` path reference
- [x] Run a clean `cargo build --workspace --all-targets` + `cargo clippy --workspace --all-targets -- -D warnings` pass (plus the wasm32 build/clippy commands for `tpt-appfront-dom`/`tpt-appfront-canvas`) to confirm Phase 17's build fixes are complete — `cargo build --workspace --all-targets`, `cargo test --workspace`, `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo clippy -p tpt-appfront-dom -p tpt-appfront-canvas --target wasm32-unknown-unknown --all-targets -- -D warnings` all exit 0
- [x] Harden `tpt-appfront-core/src/styling.rs`'s unknown-utility-class `panic!` — confirmed the `panic!` lives only in `class_macro_check`, a `const fn` invoked solely by the `class!` macro over source literals, so it is compile-time-only and unreachable with runtime/untrusted input (no crash vector for AI-agent-driven or user-editable UI). Runtime consumers of arbitrary class strings (`class_value`/`inline_style`, used by `tpt-appfront-html`) already pass unrecognized names through unchanged. `class_macro_check`'s doc comment now states this explicitly
- [x] Document the CSRF opt-in-per-client posture (`router/csrf.rs::verify` allows requests through with no CSRF cookie present) and the in-memory/per-process rate limiter caveat (`router.rs`) as intended for all deployment modes — recorded under CHANGELOG.md "Unreleased → Notes" (CSRF is opt-in per-client, `POST /command` limiter is in-memory/per-process/per-peer-IP and does not survive restarts or span instances; front multi-instance prod with shared infra)
- [x] Flesh out `CHANGELOG.md` beyond the empty skeleton — renamed stale `appfront-*` entries to `tpt-appfront-*`, and added an Unreleased section (Added/Fixed) covering Phases 14–18 (templates crate, CLI `ingest`/`add`/`doctor`, canvas virtual-scroll/styling/accesskit, dom form-controls + `unmount`, `view!` control flow, server per-peer rate limiting + ETag caching, plus the Phase 17 build-breakage fixes and the DataGrid paint fix)

## Phase 19 — 2026-08-12 Developer Experience Improvements (done)
Triggered by a "make this easier for devs, maybe better tools?" ask, plus a follow-up request for `init` presets. Plan: `C:\Users\phill\.claude\plans\can-we-make-this-majestic-rose.md`.
- [x] `view!` macro tag coverage: added `Textarea`/`Checkbox`/`Select`/`Radio` codegen to `view.rs` (`gen_node_stmt` + `chain_suffix` `on_toggle`) plus a test per tag in `crates/tpt-appfront-core/tests/view_macro.rs`; these `NodeKind`s already existed and were consumed by `tpt-appfront-dom`/`tpt-appfront-tui` but `view!` couldn't build them
- [x] `init --preset <name>` — new `crates/tpt-appfront-cli/src/presets.rs` (`login`/`dashboard`/`crud-app`/`saas-starter` + `list_presets()`) wired through `main.rs`'s `init` subcommand, plus `templates.rs` generator functions (`preset_cargo_toml`/`preset_lib_rs`) that call into `tpt-appfront-templates`'s `login_form`/`dashboard_shell`/`settings_list` with real `Signal`-backed state; `--list-presets` prints them; all four presets verified to `cargo check --target wasm32-unknown-unknown`
- [x] Wire `devtools.rs` into the dev loop — `TPT_APPFRONT_DEVTOOLS=1` env-gated `inspect_tree` call added to the scaffolded `dom_lib_rs`/`canvas_main_rs`/`tui_main_rs` templates, plus a `dev --devtools` CLI flag that sets the env var on the spawned `cargo run`/`trunk` child (via `spawn_cargo_run`/`run_in` env passing)
- [x] Friendlier CLI wrapper-level errors — `missing_tool_hint()` helper for spawn failures (`trunk`/`cargo`/`cargo-packager` not on PATH) with a clear "install it with `cargo install …`" message, and an explicit bail when `--desktop-webview` runs without a `ui/index.html`
- [x] Light IDE support — `.vscode/extensions.json` (rust-analyzer, even-better-toml, crates) and `docs/editor-setup.md` covering `view!` macro expansion and the `wasm32-unknown-unknown` caveat for `tpt-appfront-dom`/canvas; a full VS Code extension remains future/stretch work
- [x] Docs expansion — `docs/quickstart.md` gains sections for `doctor`/`benchmark`/`optimize --bundle`/webview backend/smart router/TUI target/devtools/`init --preset`/new `view!` tags; new `docs/troubleshooting.md` seeded from doctor-flagged issues and the path-dep-vs-published-version FAQ

## Cross-cutting — per-crate docs (user request)
- [x] Every crate (`tpt-appfront-core`, `-dom`, `-canvas`, `-html`, `-ai-schema`, `-server`, `-tui`, `-webview`, `-mcp`, `-templates`, `-macros`, `-cli`) now has a comprehensive `README.md` (overview, features, install, usage example, license) and a crate-scoped `CHANGELOG.md` (Keep a Changelog style, Unreleased + 0.1.0 sections)
- [x] Escape hatches & composition docs — new `docs/escape-hatches.md` formally documenting `{ expr }`/`ContainerBuilder::with` component composition and the `examples/node-graph` raw-egui escape-hatch pattern (today only discoverable via that example's own README), linked from `README.md`/`CLAUDE.md`
- [x] Custom-tag sugar in `view!` — unknown capitalized self-closing tags (e.g. `<LoginForm .../>`) expand via a fixed naming convention (`{Tag}Config` struct + snake_case fn) to a component call, matching `tpt-appfront-templates`'s existing convention with zero changes to that crate; unknown tags within edit-distance 2 of a built-in tag keep today's friendly typo error instead of silently generating a bogus call; non-self-closing custom tags stay a hard error pointing at `{ expr }` composition
- [x] `init --with <a,b,c>` — à la carte scaffold composition alongside the fixed `--preset` shortcut, combining multiple `tpt-appfront-templates` pieces into one generated project (e.g. `dashboard,settings` fills `DashboardShellConfig::content` with `settings_list`, matching `examples/templates-demo`'s proven composition)
- [x] Hot reload for `dev --tui` / `dev --desktop-webview` — both currently do a one-shot `cargo run` with no watch loop, unlike `--desktop`; reuse the existing `Watcher`/`dev_desktop_watch` poll-restart machinery (generalized off "desktop"-specific naming) for both, plus a second watched dir + `trunk build` re-run for `--desktop-webview`'s nested `ui/` app
- [x] Optimizer follow-through — canvas gets an opt-in `.auto_optimize(true)` flag that acts on `AutoOptimizer`'s `virtual_scrolling` recommendation (auto-applies `VirtualScroll` to large unconfigured `List`/`DataGrid` nodes; `texture_caching` stays recommendation-only, no cache layer exists yet to toggle); new `tpt-appfront optimize --analyze` heuristic scan (deterministic, no AST/LLM) flags unvirtualized long lists and release profiles missing the CLI's own default size flags

## Phase 20 — 2026-08-12 Platform Review Follow-ups
Tracking items from a "review platform for bugs, todos, missing features, innovation, usability/automation" ask; three parallel Explore surveys (todo.md/spec.txt/TODO-marker gaps, `tpt-appfront-core`+macros, backend crates + CLI) turned up correctness bugs, gaps, and adoption ideas. Review report: `C:\Users\phill\.claude\plans\review-platform-for-bugs-shimmering-duckling.md`.

### Correctness bugs
- [x] `#[component(memo)]` fails to compile for zero-arg components — `crates/tpt-appfront-macros/src/lib.rs:156-185` already generates `()` for the key when there are no args (the `__appfront_unit_key` ident bug is gone); verified the macro expands zero-arg components correctly
- [x] Canvas layout panics the whole window — `crates/tpt-appfront-canvas/src/app.rs:92` already `if let Err(e) = tree.compute_layout(...) { eprintln!(...); return; }` so a transient taffy failure (e.g. zero/NaN size on minimize) degrades instead of crashing the app
- [x] `ContainerBuilder::portal` silently swallows malformed bodies — `crates/tpt-appfront-core/src/ui_tree.rs:548` already `.expect(...)`s with a clear message when the portal body has ≠1 root child, so the "blank overlay" mistake now errors loudly
- [x] MCP server reads stdin lines unbounded — `crates/tpt-appfront-mcp/src/lib.rs:106-162` already bounds each newline frame to `MAX_LINE_BYTES` (16 KiB) before `serde_json::from_str`, with an `oversized_stdio_frames_are_dropped_without_crashing` test
- [x] `reconcile_keys` is O(n·m) — `crates/tpt-appfront-core/src/reconcile.rs:59-108` already uses `HashSet`/`HashMap` membership (`O(n+m)`), and `warn_on_duplicate_keys` emits a debug-build warning for the documented undefined-duplicate-key case (test: `large_full_reorder_stays_correct_and_fast` at 20k items)
- [x] No effect cleanup hook in the reactive system — `crates/tpt-appfront-core/src/signal.rs:475` already has `pub fn on_cleanup(...)` (SolidJS-style `on_cleanup` teardown) wired into `create_effect`'s run/drop cycle; `on_cleanup_runs_before_next_run_and_on_drop` test covers it

### Gaps / fragile heuristics
- [x] Expose `Signal::set_if_changed` publicly — `crates/tpt-appfront-core/src/signal.rs:355` already `pub fn set_if_changed` (and the private one used by `create_memo` was removed in favor of it); `set_if_changed_does_not_notify_on_unchanged_value` test covers it
- [x] `Signal<T>` requires `T: Clone` unconditionally — no longer a practical tax: `new`/`set`/`with` only require `T: 'static` (via `impl<T: 'static> Signal<T>`), so write-only/large state is read via `with()` without cloning; only `get()` needs `T: Clone` by design (documented on the method)
- [x] `is_literal_expr` in `view!` misses negative numeric literals — `crates/tpt-appfront-macros/src/view.rs:641` already matches `Expr::Unary(Neg(_))` over an `Expr::Lit`, so `level={-1}` is correctly treated as literal and hoisted
- [x] AI-schema page-title heuristic can misname a page — `crates/tpt-appfront-ai-schema/src/ai_schema.rs:48-57` already prefers an explicit `ui.meta.ai.title` override before falling back to the first `DataElement`'s text
- [x] `tpt-appfront-dom`'s `element_key` casts an `Element` pointer to `u32` — **FIXED** (`crates/tpt-appfront-dom/src/lib.rs:1098`): `node_id` now returns `u64` and `LiveClosures` is keyed by `u64`, widening the pointer before hashing so it stays correct under wasm64/`memory64`; hydration's id-map-miss dev warning was already present (`lib.rs:1405-1420`)
- [x] Webview IPC ACL violations only go to `eprintln!` — `crates/tpt-appfront-webview/src/lib.rs:488` `handle_ipc` already takes an `on_reject: &dyn Fn(IpcRejection)` hook invoked at every rejection point in addition to the crate's own log output
- [x] `client_kind.rs` AI-agent UA detection is a hardcoded substring list — `crates/tpt-appfront-server/src/client_kind.rs:72,96` already supports `extra_ai_agents: &[&str]` via `SmartRouterBuilder::extra_ai_agents(...)`, exercised by `extra_ai_agents_extends_detection`
- [x] Rate limiting silently defeated behind a reverse proxy — `crates/tpt-appfront-server/src/router.rs:311` already has `SmartRouterBuilder::trust_forwarded_for(bool)` (default false, documented as off unless the proxy is trusted); `PeerIpKeyExtractor` is used when enabled

### Missing features (cross-cutting)
- [x] No `Image`/`Media`/`Link` `NodeKind` anywhere in `UITree` — added `NodeKind::Image { src, alt, on_click }` / `Link { href, text, on_click }` / `Media { src, alt, media_type }` + `MediaType` (`crates/tpt-appfront-core/src/ui_tree.rs:85-108`, builder methods `:562-585`). Rendered by every backend: dom emits `<img>`/`<a>`/`<audio>`/`<video>` + `aria-label` (`crates/tpt-appfront-dom/src/lib.rs:998`), html the equivalent tags + `aria-label` (`crates/tpt-appfront-html/src/lib.rs:225`), canvas via label rendering + AccessKit (`layout.rs`/`paint.rs`), tui via text-fallback rendering (`crates/tpt-appfront-tui/src/lib.rs:133-135`), ai-schema via `ImageObject`/`WebPageElement`/`AudioObject`/`VideoObject` (`json_ld.rs`). Covered by `view!` macro parsing tests (`crates/tpt-appfront-core/tests/view_macro.rs`); no dedicated per-backend render tests yet
- [x] Accessibility (ARIA/roles/alt/label-`for`) — closed the remaining gaps (`crates/tpt-appfront-html/src/lib.rs` + `crates/tpt-appfront-dom/src/lib.rs`): form controls (`Input`/`Textarea`/`Select`/`Checkbox`/`Radio`) now emit a stable `id` (`af-<data_appfront_id>`) and `Checkbox`/`Radio` use explicit `<label for>` association (in addition to the wrapping `<label>`), `Select` surfaces `meta.ai.description` as `aria-label` (native role is already correct, so only the accessible name was missing), and `DataGrid` now renders `role="grid"` on the table plus `role="row"`/`role="columnheader"`/`role="gridcell"` on its rows/headers/cells in both backends (tested at html `:515-565`). `tpt-appfront doctor --a11y` lint subcommand added (`crates/tpt-appfront-cli/src/main.rs`): a deterministic, no-AST/LLM heuristic scan that flags `image`/`link`/`button` builder calls missing an accessible name; informational, always `Ok` (fits the existing `doctor`/`optimize --analyze` pattern)
- [x] `tpt-appfront-dom` standalone wasm build failed on a missing `web-sys` `console` feature — `hydrate_node`'s `#[cfg(debug_assertions)]` `web_sys::console::warn_1` calls referenced the feature, which was never enabled in `tpt-appfront-dom/Cargo.toml`; it only compiled in the workspace because `tpt-appfront-canvas`/`webview` enable `console` via feature unification. Added `"console"` to `tpt-appfront-dom`'s `web-sys` features so `cargo build -p tpt-appfront-dom --target wasm32-unknown-unknown` and its `--all-targets` clippy now pass standalone (no transitive-crate reliance)
- [x] No virtual-scroll support in `tpt-appfront-tui` — `tui_window()` (`crates/tpt-appfront-tui/src/lib.rs:145-159`) reinterprets px-based `VirtualScroll` as row-based windowing, used by `List` (`:304-305`) and `DataGrid` (`:321,325`), falling back to the full range when unset. Test: `list_virtual_scroll_renders_only_window` (`:853-871`) — `List` only, no dedicated `DataGrid` virtual-scroll test yet
- [x] `tpt-appfront-server` is never wired into the CLI — `InitTarget::Server` scaffolds a smart-router server crate via `init --target server` (`crates/tpt-appfront-cli/src/main.rs:32,410,430,455`)
- [x] Canvas's `AutoOptimizer` auto-virtualization (Phase 19) is only wired into `CanvasApp` — **FIXED**: a shared `tpt_appfront_core::apply_auto_virtual_scroll(ui, viewport_height)` helper now drives auto-windowing for all three backends — `CanvasApp::auto_optimize` (`app.rs:81`), `TuiDriver::auto_optimize` + `run_with`/`render_to_buffer_with` (`tpt-appfront-tui/src/lib.rs`), and `render_with`/`mount_router_with` (`tpt-appfront-dom/src/lib.rs`, applying it against `container.client_height()`). Tests: `auto_virtual_scroll_windows_large_unconfigured_list`, `auto_virtual_scroll_respects_existing_config` (TUI)

### Notes (not new work, context only)
- spec.txt's "Layer 5: AI-Powered Optimization" (AI suggesting UI structure improvements) has no todo.md item at all — appears to have been dropped from planning entirely rather than deferred; flagging here in case it should be triaged as stretch work

## Verification checkpoints
- [ ] Phase 3: `trunk serve` runs counter app in browser; devtools confirms only the text node mutates on click
- [x] Phase 4: same counter `UITree` shape renders via native `cargo run` (`examples/counter-canvas`, manually driven: screenshotted, simulated a click, confirmed count incremented 8→9); WASM canvas in-browser only build-checked (`cargo check --target wasm32-unknown-unknown`), not run in an actual browser this session
- [x] Phase 6/7: `tpt-appfront-server/tests/e2e_test.rs` covers this (spins up the router via `axum::serve` on a background thread, then hits it with `ureq`: Googlebot UA → semantic HTML, plain browser UA → WASM shell, AI-agent UA → JSON-LD+AI-Schema, `?client=` query override honored). The earlier "hangs indefinitely" note was **not** a sandbox/firewall restriction — root-caused with a raw-`std::net::TcpStream` probe test (bypassing `ureq`) that isolated the hang to the server side: `ServerGuard::start` converted a `std::net::TcpListener` into a `tokio::net::TcpListener` via `from_std()` without first calling `listener.set_nonblocking(true)`, which tokio's docs require — on Windows this leaves the listener not properly registered with the I/O driver, so accepted connections are never polled and the client-side read times out. Fixed with one `set_nonblocking(true)` call before `from_std()`; all 4 tests now pass (`cargo test -p tpt-appfront-server --test e2e_test`, 1.43s)
- [x] `cargo test --workspace` (native, now including the 4 e2e tests, all pass after the `set_nonblocking` fix above) and `cargo clippy --workspace --all-targets -- -D warnings` (native) both pass; `cargo clippy -p tpt-appfront-dom -p tpt-appfront-canvas --target wasm32-unknown-unknown --all-targets -- -D warnings` also passes. Fixed one real failure found along the way: `crates/tpt-appfront-canvas/src/layout.rs:118` used a bare `percent(1.0)` float literal that newer rustc flags as an ambiguous `f32`-fallback (`float_literal_f32_fallback`, promoted to a hard error under `-D warnings`) — changed to `percent(1.0_f32)`
- [x] Re-verified after the Phase 1/3/6/7 fixes above (`Signal::with`, `reactive_text` handle return, `to_ai_schema_value` `Result`, server hardening middleware): `cargo build --workspace --all-targets`, `cargo test --workspace --exclude tpt-appfront-server` (36 tests pass, including a new `with_reads_without_cloning_and_still_tracks` case), `tpt-appfront-server`'s in-crate `--lib` tests (17 tests, exercised via `oneshot` so unaffected by the sandbox socket issue below), `cargo clippy --workspace --all-targets -- -D warnings`, and `cargo clippy -p tpt-appfront-dom -p tpt-appfront-canvas --target wasm32-unknown-unknown --all-targets -- -D warnings` all pass clean

## Phase 21 — 2026-08-12 Dev-Tooling Hygiene (CI + local)
Enforcement envelope around the existing feature tooling: formatting config + CI
fmt/doc/MSRV jobs + local pre-commit hook + a `just` task runner + `tpt-appfront fmt`/`lint`
convenience wrappers and `doctor` hygiene checks. Plan: `.kilo/plans/1786476730506-ci-local-hygiene-plan.md`.
- [x] `rustfmt.toml` at repo root enabling `edition = "2021"` + the unstable options `group_imports = "StdExternalCrate"`, `imports_granularity = "Module"`, `format_code_in_doc_comments = true`; because those options are nightly-only, formatting is enforced via `cargo +nightly fmt` in the CI `fmt` job, `just fmt`/`fmt-check`, and `tpt-appfront fmt` (a one-time `cargo +nightly fmt --all` normalization commit was applied; `rustup toolchain install nightly` is the only prerequisite)
- [x] `rust-version = "1.85"` pinned in `[workspace.package]` and a CI `msrv` job that builds the workspace with that exact toolchain
- [x] CI: new `fmt` job (`cargo +nightly fmt --all --check` + per-`examples/*/Cargo.toml` nightly check), `doc` job (`RUSTDOCFLAGS="-D warnings" cargo doc --workspace --no-deps --exclude tpt-appfront-webview` — the codebase's pre-existing broken intra-doc links were fixed so `-D warnings` now passes), and PR `concurrency` group to cancel superseded runs
- [x] Local pre-commit hook: `scripts/git-hooks/pre-commit` (bash) running fmt-check + clippy `-D warnings` + test, wired via `git config core.hooksPath scripts/git-hooks`; `scripts/install-hooks.sh` sets it up
- [x] `justfile` task runner: `fmt`, `fmt-check`, `lint`, `test`, `doc`, `msrv`, `ci`, `examples` targets (fmt uses nightly); documented in `docs/quickstart.md` + `docs/editor-setup.md`
- [x] `tpt-appfront fmt` / `tpt-appfront lint` subcommands (`fmt` runs `cargo +nightly fmt` across the workspace + each `examples/*/Cargo.toml`); `doctor` now also reports `cargo fmt` availability, nightly-rustfmt presence, `core.hooksPath`, and whether the workspace is formatted

## Phase 22 — `tpt-appfront generate --llm` (scoped, not yet implemented)
Live LLM-backed `generate --llm` mode, scoped out from the Phase 11/15 stretch item. Full design: `C:\Users\phill\.claude\plans\what-tasks-remain-and-replicated-dusk.md`. Decisions: pluggable `LlmProvider` trait shipping 5 built-in providers — Anthropic, OpenAI, OpenRouter, Grok (xAI), and Ollama (local) — gated behind an optional `llm` Cargo feature so the default install stays network-free, and `syn`-based syntax-check-only validation of the returned snippet (no full compile-check).
- [x] Add `llm` optional feature + deps (`ureq`, `serde`, `serde_json`, `syn`) to `crates/tpt-appfront-cli/Cargo.toml`
- [x] `LlmProvider` trait + 2 implementations covering all 5 providers (new `crates/tpt-appfront-cli/src/llm/mod.rs`, `#[cfg(feature = "llm")]`): `anthropic.rs` (native Messages API) and `openai_compat.rs` (generic `chat/completions` shape, config-table-driven for OpenAI/OpenRouter/Grok/Ollama — base URL, API-key env var, default model per provider via `PROVIDER_CONFIGS`; Ollama needs no key but requires an explicit `--model`)
- [x] Prompt construction: system prompt advertises `view!`'s supported tag set + few-shot examples reused from `generate.rs`'s `PATTERNS` (single source of truth for the few-shots). Note: the tag list lives in `llm/mod.rs::VIEW_TAGS` as a documented mirror of `view.rs`'s `TAGS` — a proc-macro crate can't `pub` a `const`, and `tpt-appfront-macros` must not depend on `core` (would be a cycle), so the prompt can't reference the macro's `TAGS` directly. `build_system_prompt`'s test asserts every `VIEW_TAGS` entry appears in the prompt, keeping the two lists honest.
- [x] Response handling: extract the first fenced code block, syntax-check with `syn` (`syn::parse_file`), print/write as-is on success or with a `// WARNING: failed syntax validation` banner on failure (never hard-error on a close-but-imperfect LLM response)
- [x] CLI wiring in `crates/tpt-appfront-cli/src/main.rs`: `Generate` gains `--llm`/`--provider` (`anthropic`/`openai`/`openrouter`/`grok`/`ollama`, defaults to `anthropic`)/`--model`/`--base-url`; clear errors when `--llm` is passed without the feature compiled in, without the selected provider's API key set, or `--provider ollama` without `--model` — no silent fallback to the offline generator or another provider
- [x] Tests: network-free unit tests for prompt construction, the 5-provider config table (base URL/key-env/default-model resolution), and response/`syn`-validation (canned strings covering valid/malformed/prose-wrapped snippets); one `#[ignore]`d real-API integration test per provider (anthropic/openai/openrouter/grok/ollama), never wired into CI
- [x] CI: `cargo clippy -p tpt-appfront-cli --features llm --all-targets -- -D warnings` step so the feature path doesn't bit-rot without requiring network access in CI
- [x] Docs: `docs/quickstart.md` gains a `generate --llm` subsection (optional/feature-gated, listing all 5 `--provider` choices with their key env vars, network egress called out explicitly); flip this phase to done once landed
