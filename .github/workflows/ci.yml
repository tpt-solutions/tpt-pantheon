name: CI

on:
  push:
    branches: ["**"]
  pull_request:

permissions:
  contents: read

env:
  CARGO_TERM_COLOR: always
  RUST_BACKTRACE: 1
  SCCACHE_GHA_ENABLED: "true"

jobs:
  # ---------------------------------------------------------------------------
  # Core engine: the existing 455-test suite (unit + Phase 3 multi-node).
  # ---------------------------------------------------------------------------
  keystone:
    name: keystone (cargo test)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: mozilla-actions/sccache-action@v0.0.6
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: tpt-keystone
      - name: Run tests
        working-directory: tpt-keystone
        run: cargo test

  # ---------------------------------------------------------------------------
  # Library crates: build + test each independently (no shared workspace).
  # ---------------------------------------------------------------------------
  rust-libs:
    name: ${{ matrix.crate }} (cargo build + test)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        crate: [tpt-keystone-cli, tpt-keystone-sdk, tpt-keystone-harbor, tpt-keystone-operator]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - name: Install unixODBC (tpt-keystone-harbor's odbc-api dep links against it)
        if: matrix.crate == 'tpt-keystone-harbor'
        run: sudo apt-get update && sudo apt-get install -y unixodbc-dev
      - uses: mozilla-actions/sccache-action@v0.0.6
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: ${{ matrix.crate }}
      - name: Build
        working-directory: ${{ matrix.crate }}
        run: cargo build
      - name: Test
        working-directory: ${{ matrix.crate }}
        run: cargo test
      - name: Test (ffi feature)
        if: matrix.crate == 'tpt-keystone-sdk'
        working-directory: ${{ matrix.crate }}
        run: cargo test --features ffi

  # ---------------------------------------------------------------------------
  # Canvas: WASM build only (no meaningful native test path).
  # ---------------------------------------------------------------------------
  canvas:
    name: tpt-keystone-canvas (wasm32 build)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - uses: mozilla-actions/sccache-action@v0.0.6
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: tpt-keystone-canvas
      - name: Build wasm32
        working-directory: tpt-keystone-canvas
        run: cargo build --target wasm32-unknown-unknown

  # ---------------------------------------------------------------------------
  # Clippy: report-only lint pass across every Rust crate. Not a hard gate yet
  # (tpt-keystone alone currently has ~246 pre-existing warnings to work
  # through), so failures here warn but don't fail the workflow.
  # ---------------------------------------------------------------------------
  clippy:
    name: ${{ matrix.crate }} (cargo clippy)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        crate: [tpt-keystone, tpt-keystone-cli, tpt-keystone-sdk, tpt-keystone-harbor, tpt-keystone-operator, tpt-keystone-canvas]
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
          targets: ${{ matrix.crate == 'tpt-keystone-canvas' && 'wasm32-unknown-unknown' || '' }}
      - uses: mozilla-actions/sccache-action@v0.0.6
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: ${{ matrix.crate }}
      - name: Clippy (report-only)
        working-directory: ${{ matrix.crate }}
        continue-on-error: true
        run: |
          if [ "${{ matrix.crate }}" = "tpt-keystone-canvas" ]; then
            cargo clippy --target wasm32-unknown-unknown --all-targets -- -W clippy::all
          else
            cargo clippy --all-targets -- -W clippy::all
          fi

  # ---------------------------------------------------------------------------
  # TypeScript SDKs: build (tsc) + node --test.
  # ---------------------------------------------------------------------------
  javascript:
    name: ${{ matrix.pkg }} (npm test)
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        pkg: [sdk-web, sdk-server, sdk-edge]
    defaults:
      run:
        working-directory: packages/${{ matrix.pkg }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: npm
          cache-dependency-path: packages/${{ matrix.pkg }}/package-lock.json
      - run: npm ci
      - run: npm test

  # ---------------------------------------------------------------------------
  # Build the core engine once; the live Go/Python jobs download it as an
  # artifact so they don't each recompile the whole engine.
  # ---------------------------------------------------------------------------
  build-keystone:
    name: build keystone binary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: mozilla-actions/sccache-action@v0.0.6
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: tpt-keystone
      - name: Build (debug)
        working-directory: tpt-keystone
        run: cargo build
      - name: Upload binary
        uses: actions/upload-artifact@v4
        with:
          name: keystone-bin
          path: tpt-keystone/target/debug/tpt-keystone
          if-no-files-found: error

  # ---------------------------------------------------------------------------
  # Python SDK: offline pytest, then live_smoke.py against a real server
  # (started in-job from the artifact above on the standard 5432 port).
  # ---------------------------------------------------------------------------
  python:
    name: sdk-python (pytest + live)
    runs-on: ubuntu-latest
    needs: [build-keystone]
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Download keystone binary
        uses: actions/download-artifact@v4
        with:
          name: keystone-bin
          path: keystone-bin
      - name: Start keystone
        run: |
          chmod +x keystone-bin/tpt-keystone
          TPT_PG_ADDR=0.0.0.0:5432 ./keystone-bin/tpt-keystone > keystone.log 2>&1 &
          echo $! > keystone.pid
      - name: Wait for keystone
        run: |
          for i in $(seq 1 60); do
            if (exec 3<>/dev/tcp/127.0.0.1/5432) 2>/dev/null; then
              echo "keystone up"; break
            fi
            sleep 1
          done
          (exec 3<>/dev/tcp/127.0.0.1/5432) 2>/dev/null || { echo "did not start"; cat keystone.log; exit 1; }
      - name: Install SDK + test deps
        working-directory: sdk-python
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"
          pip install pandas
      - name: Offline pytest
        working-directory: sdk-python
        run: python -m pytest -v
      - name: Live smoke test
        working-directory: sdk-python
        run: python tests/live_smoke.py
      - name: Stop keystone
        if: always()
        run: kill "$(cat keystone.pid)" || true

  # ---------------------------------------------------------------------------
  # Dart/Flutter SDK: offline `dart test` (fully mocked, no live server needed).
  # ---------------------------------------------------------------------------
  dart:
    name: tpt_sdk (dart test)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: tpt_sdk
    steps:
      - uses: actions/checkout@v4
      - uses: dart-lang/setup-dart@v1
        with:
          sdk: stable
      - run: dart pub get
      - run: dart test

  # ---------------------------------------------------------------------------
  # Go SDK: offline `go test ./...`, then `-tags live` against the real
  # server started in-job from the artifact above.
  # ---------------------------------------------------------------------------
  go:
    name: sdk-go (go test + live)
    runs-on: ubuntu-latest
    needs: [build-keystone]
    permissions:
      contents: read
      actions: read
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.21"
      - name: Download keystone binary
        uses: actions/download-artifact@v4
        with:
          name: keystone-bin
          path: keystone-bin
      - name: Start keystone
        run: |
          chmod +x keystone-bin/tpt-keystone
          TPT_PG_ADDR=0.0.0.0:5432 ./keystone-bin/tpt-keystone > keystone.log 2>&1 &
          echo $! > keystone.pid
      - name: Wait for keystone
        run: |
          for i in $(seq 1 60); do
            if (exec 3<>/dev/tcp/127.0.0.1/5432) 2>/dev/null; then
              echo "keystone up"; break
            fi
            sleep 1
          done
          (exec 3<>/dev/tcp/127.0.0.1/5432) 2>/dev/null || { echo "did not start"; cat keystone.log; exit 1; }
      - name: Offline tests
        working-directory: sdk-go
        run: go test ./...
      - name: Live tests (-tags live)
        working-directory: sdk-go
        run: go test -tags live ./...
      - name: Stop keystone
        if: always()
        run: kill "$(cat keystone.pid)" || true
