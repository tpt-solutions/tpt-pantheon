[package]
name = "counter-tui"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
tpt-appfront-core = { path = "../../crates/tpt-appfront-core" }
tpt-appfront-tui = { path = "../../crates/tpt-appfront-tui" }
